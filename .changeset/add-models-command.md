---
"wandler": minor
---

Add `wandler models` CLI command to list verified models with their capabilities. The catalog is built from a model registry, cached locally with a 1-hour TTL, and refreshed from GitHub. Agents can now query available LLM, embedding, and STT models without leaving the CLI.
