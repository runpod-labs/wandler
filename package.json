{
  "name": "@wandler/monorepo",
  "private": true,
  "workspaces": [
    "server",
    "site",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "turbo": "^2.5.4"
  }
}
